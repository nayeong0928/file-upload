# PRD 기반 단계별 구현 프롬프트

`PRD.md`의 요구사항을 Claude Code로 순서대로 구현하기 위한 4단계 프롬프트입니다. 각 단계를 별도 세션(또는 순서대로 이어서)에 그대로 붙여넣어 사용하세요. 이전 단계가 끝난 뒤 다음 단계를 시작하는 것을 전제로 작성했습니다.

---

## 1단계 — 프로젝트 기반 설정 & 데이터 모델

```
이 저장소의 PRD.md를 읽고, 파일 업로드 확장자 차단 시스템의 기반을 설정해줘.

이번 단계 범위:
1. 기술 스택을 정한다. 이미 정해진 스택이 없다면 백엔드/DB/프론트엔드 조합을 제안하고 나에게 확인받은 뒤 진행해줘 (예: Node.js/Express + SQLite + React, 또는 Spring Boot + H2 + Thymeleaf 등 — 특별한 제약은 없음).
2. PRD.md 6장(데이터 모델)을 기준으로 blocked_extension 테이블을 생성한다.
   - extension은 대소문자 무시 unique 제약을 반드시 건다 (동시 추가 시 race condition 방지 목적).
   - FIXED 7종(bat, cmd, com, cpl, exe, scr, js)을 is_blocked=false 상태로 seed 데이터로 넣는다.
   - upload_file 테이블(업로드 이력)은 선택 사항이니 만들 경우 PRD 6.2 스키마를 따른다.
3. 서버/클라이언트가 로컬에서 기동되는 최소 골격을 만든다 (라우팅, DB 연결까지만. 아직 화면 UI나 API 로직은 만들지 않는다).
4. README에 실행 방법(서버 기동, DB 초기화 명령)을 간단히 적어줘.

이번 단계에서 하지 않을 것: 정책 관리 화면, 파일 업로드 화면, 검증 로직, API 엔드포인트 구현 (다음 단계에서 진행).

완료 기준: 서버가 정상 기동되고, blocked_extension 테이블에 FIXED 7종이 is_blocked=false로 조회되는 것을 직접 확인해줘.
```

---

## 2단계 — 확장자 차단 정책 관리 기능 (관리자)

```
이 저장소의 PRD.md를 읽고, 4.1 확장자 차단 정책 관리 화면과 관련 API를 구현해줘. (1단계에서 만든 DB 스키마/프로젝트 구조를 그대로 사용한다.)

구현 대상 (PRD.md FR-ID 기준):
- FR-A1-1~5: 고정 확장자 7종 체크박스. 기본 unchecked, 체크/해제 즉시 DB 저장, 새로고침 후에도 유지, 커스텀 목록에는 노출 안 함.
- FR-A2-1~7: 커스텀 확장자 입력(최대 20자) → "추가" 시 저장 및 목록 표시, 최대 200개, X클릭 시 즉시 삭제, 중복 추가 방지, 고정 확장자와 겹치면 거부, 영문/숫자만 허용하고 소문자로 정규화.
- 에러 메시지는 PRD.md 4.4 표에 정의된 문구를 그대로 사용해줘 (임의로 문구를 바꾸지 말 것).
- 화면은 관리자 전용 독립 진입점으로 만든다. 다른 화면(업로드 화면)으로 이동하는 버튼/링크는 두지 않는다 (PRD.md 4.1의 주석 참고).
- API는 PRD.md 7장에 정의된 엔드포인트 형태(GET/PATCH fixed, GET/POST/DELETE custom)를 참고해서 구현한다.

이번 단계에서 하지 않을 것: 파일 업로드 화면, 실제 업로드 검증/처리 로직 (다음 단계에서 진행).

완료 기준: 아래 시나리오를 직접 실행해서 확인해줘.
- 고정 확장자 체크 후 새로고침해도 체크 상태 유지
- 커스텀 확장자 "sh" 추가 → 목록에 표시 → 다시 "sh" 추가 시도 시 "이미 등록된 확장자입니다" 노출
- 고정 확장자에 있는 값(예: exe)을 커스텀으로 추가 시도 시 "고정 확장자에 있는 확장자입니다" 노출
- 커스텀 확장자 X 클릭 시 목록에서 즉시 사라지고 DB에서도 삭제됨
```

---

## 3단계 — 파일 업로드 기능 (사용자)

```
이 저장소의 PRD.md를 읽고, 4.2 파일 업로드 화면과 4.3 실제 업로드 처리 로직을 구현해줘. (2단계에서 만든 정책 데이터를 그대로 사용해 검증한다.)

구현 대상:
- 사용자가 파일을 첨부해 업로드하는 화면을 관리자 화면과 독립된 별도 진입점으로 만든다. 정책 관리 화면으로/에서 이동하는 경로는 없다.
- PRD.md 4.2.1 "검증 순서"에 정의된 순서를 정확히 그대로 구현한다:
  1) 매직 넘버 기반 실제 파일 형식 판별 (클라이언트가 보낸 MIME 타입은 신뢰하지 않는다)
  2) 이중 확장자 검사
  3) 확장자 위장 검사 (파일명에 확장자가 없으면 이 단계는 건너뜀)
  4) 차단 정책 검사 (실제 확장자 기준, 2단계에서 관리자가 설정한 최신 정책을 그때그때 조회)
  5) 특수 파일명 치환: 확장자 없음 / '.'으로 시작 / 매우 긴 파일명 → 파일명을 초 단위 현재 시간, 확장자는 실제 형식으로 치환
  6) 저장 및 성공 응답
  반드시 4번(차단 정책 검사)을 통과한 파일에 대해서만 5번 치환·저장을 수행해야 한다 — 파일명 치환으로 차단 정책을 우회할 수 없어야 한다 (PRD.md의 보안 원칙 참고).
- 거부되는 모든 경우 PRD.md 4.4 에러 메시지 표의 문구를 그대로 사용해 사유를 보여준다.
- FR-C4: 저장 경로에 원본 파일명을 그대로 쓰지 않고, path traversal이 불가능하도록 처리한다.

완료 기준: 아래 케이스를 직접 만들어 테스트하고 결과를 알려줘.
- 정상 파일(예: 정상 png) 업로드 → 성공
- 2단계에서 체크/등록한 차단 확장자 파일 업로드 → "차단된 확장자입니다: .xxx" 로 거부
- 이중 확장자 파일(예: invoice.pdf.exe) → 이중 확장자 사유로 거부
- 실제 내용과 확장자가 다른 파일(예: exe를 jpg로 이름만 바꾼 파일) → 확장자 위장 사유로 거부
- 확장자 없는 파일 / .env 같은 dot 파일 / 매우 긴 파일명 파일 → 정책 위반이 없다면 타임스탬프.실제확장자로 저장되는지 확인
```

---

## 4단계 — 통합 검증 & 마무리

```
이 저장소의 PRD.md를 기준으로 지금까지 구현한 기능 전체를 점검하고 다듬어줘.

작업 내용:
1. PRD.md 3장(사용자 시나리오)의 관리자/사용자 흐름을 처음부터 끝까지 직접 실행해서 확인한다.
2. PRD.md의 모든 FR-ID(FR-A1-*, FR-A2-*, FR-B1-*, FR-C1~C4)를 체크리스트로 정리하고, 각 항목이 실제로 만족되는지 하나씩 확인한다. 빠진 부분이 있으면 이번 단계에서 보완한다.
3. PRD.md 5장 비기능요구사항(매직바이트 검증, 동시성 unique 제약, path traversal 방지 등)이 실제로 지켜지고 있는지 코드 레벨에서 재확인한다.
4. PRD.md 8장 오픈 이슈 중 이미 구현 과정에서 임의로 결정한 값(예: 매우 긴 파일명 기준 글자 수, 커스텀 확장자 입력 형식 등)이 있다면, 실제 적용한 값을 PRD.md 8장에 업데이트해서 기록에 남긴다.
5. 가능하면 핵심 시나리오(정상 업로드, 각 거부 사유별 케이스, 정책 체크/추가/삭제)에 대한 자동화 테스트를 추가한다.

완료 기준: FR 체크리스트를 전부 통과했는지와, 남은 이슈가 있다면 무엇인지 요약해서 알려줘.
```
