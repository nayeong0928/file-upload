# 파일 업로드 확장자 차단 시스템 PRD

## 1. 개요

### 1.1 배경 및 목적
악성 실행 파일(예: `.exe`, `.bat`)이나 위장된 파일(이중 확장자, 확장자 위조 등)이 업로드되어 서버 침해나 사용자 피해로 이어지는 사고를 방지한다. 관리자는 차단할 확장자를 화면에서 관리하고, 이 정책은 실제 파일 업로드 처리 시점에 그대로 적용되어야 한다.

### 1.2 대상 사용자
- **관리자**: 확장자 차단 정책을 설정/관리
- **일반 사용자**: 정책이 적용된 화면에서 파일을 업로드

### 1.3 범위
- A. 확장자 차단 정책 관리 화면 (고정 확장자, 커스텀 확장자) — 관리자 전용, 독립된 진입점
- B. 파일 업로드 화면 및 실제 업로드 처리 (검증, 저장) — 사용자 전용, 독립된 진입점

두 화면은 서로 이동 경로로 연결되지 않는다. 관리자는 정책 관리 화면에, 사용자는 파일 업로드 화면에 각각 별도로 진입한다.

---

## 2. 용어 정의

| 용어 | 정의 |
|---|---|
| 고정 확장자 | 시스템이 미리 정의해 제공하는 자주 차단되는 위험 확장자 7종: `bat, cmd, com, cpl, exe, scr, js`. 관리자가 체크박스로 차단 여부를 토글한다. |
| 커스텀 확장자 | 관리자가 직접 입력해 추가하는 차단 확장자. 최대 200개. |
| 차단 정책 | 현재 차단 대상 확장자 전체 집합 = 체크된 고정 확장자 ∪ 등록된 커스텀 확장자 |
| 실제 확장자 | 파일 시그니처(매직 넘버) 등 콘텐츠 분석을 통해 판별한, 파일이 실제로 해당하는 형식의 확장자. 사용자가 붙인 파일명 확장자와 다를 수 있다. |

---

## 3. 사용자 시나리오

### 3.1 관리자 시나리오
1. 관리자가 정책 관리 화면에 진입한다.
2. 고정 확장자 중 차단하고 싶은 항목을 체크한다 → 즉시 DB에 반영된다.
3. 추가로 차단하고 싶은 확장자를 커스텀 목록에 입력하고 "추가"한다. 더 이상 차단할 필요가 없는 항목은 X로 삭제한다.
4. 별도의 저장/적용 조작 없이, 화면에서의 모든 변경은 즉시 정책에 반영된다.

### 3.2 사용자 시나리오
1. 사용자가 파일 업로드 화면에 진입한다.
2. 파일을 첨부한다.
3. 시스템이 (관리자가 설정해 둔) 현재 정책 기준으로 파일을 검증한다.
   - 차단 대상이면 사유와 함께 업로드가 거부된다.
   - 정상 파일이면 저장되고 성공 처리된다.

---

## 4. 기능 요구사항

### 4.1 확장자 차단 정책 관리 화면

> 관리자가 별도 진입점으로 접근하는 관리자 전용 화면이다. 다른 화면으로 이동하는 버튼(적용 등)은 두지 않는다 — 체크/추가/삭제 시점에 이미 DB에 반영되므로 화면을 벗어나면 그대로 정책이 확정된 상태다.

#### 4.1.1 고정 확장자
| ID | 요구사항 |
|---|---|
| FR-A1-1 | `bat, cmd, com, cpl, exe, scr, js` 7종을 체크박스 리스트로 제공한다. |
| FR-A1-2 | 최초 상태는 전부 **unchecked**(차단 안 함)이다. |
| FR-A1-3 | 체크/해제하는 즉시 DB에 저장한다 (별도 저장 버튼 없음). |
| FR-A1-4 | 새로고침 후에도 저장된 체크 상태가 그대로 유지된다. |
| FR-A1-5 | 고정 확장자 7종은 커스텀 확장자 목록 영역에는 노출되지 않는다 (두 영역은 시각적으로 분리). |

#### 4.1.2 커스텀 확장자
| ID | 요구사항 |
|---|---|
| FR-A2-1 | 입력 필드는 최대 20자까지 입력 가능하다. |
| FR-A2-2 | "추가" 클릭 시 아래 검증(FR-A2-4~7)을 통과하면 DB에 저장하고 목록에 표시한다. |
| FR-A2-3 | 커스텀 확장자는 최대 200개까지 등록 가능하다. 200개 초과 시 "추가"는 실패하며 "최대 200개까지 등록할 수 있습니다" 메시지를 노출한다. |
| FR-A2-4 | 항목별 **X** 클릭 시 즉시 DB에서 삭제하고 목록에서 제거한다. |
| FR-A2-5 | **중복 방지**: 이미 등록된 커스텀 확장자와 대소문자 무관하게 동일한 값을 추가 시도하면 저장하지 않고 "이미 등록된 확장자입니다" 메시지를 노출한다. |
| FR-A2-6 | **고정 확장자와의 충돌**: 입력값이 고정 확장자 7종 중 하나와 대소문자 무관하게 일치하면 저장하지 않고 "고정 확장자에 있는 확장자입니다" 메시지를 노출한다. |
| FR-A2-7 | 입력값은 영문/숫자만 허용하고 앞뒤 공백은 trim, 저장 시 소문자로 정규화한다. 빈 값 또는 형식에 맞지 않는 입력은 추가할 수 없다. *(가정 — 8.2 참조)* |

### 4.2 파일 업로드 화면

> 사용자가 별도 진입점으로 접근하는 사용자 전용 화면이다. 정책 관리 화면에서 넘어오는 경로는 없으며, 진입 시점마다 서버에 저장된 최신 차단 정책을 기준으로 검증한다.

| ID | 요구사항 |
|---|---|
| FR-B1-1 | 파일을 첨부할 수 있는 버튼을 제공한다. |
| FR-B1-2 | 업로드 시 아래 **4.2.1 검증 순서**에 따라 파일을 검사한다. |
| FR-B1-3 | 거부되는 모든 경우, 사용자에게 명확한 거부 사유를 표시한다 (4.4 에러 메시지 참조). |
| FR-B1-4 | 다음 조건에 해당하는 파일은 저장 전 파일명을 **현재 시간(초 단위 Unix timestamp)**으로, 확장자는 **실제 파일 형식**으로 치환한다: 확장자가 없는 파일, 점(`.`)으로 시작하는 파일(예: `.env`), 매우 긴 파일명. |

#### 4.2.1 검증 순서
업로드된 파일은 아래 순서로 검사하며, 하나라도 실패하면 즉시 거부하고 이후 단계는 수행하지 않는다.

1. **실제 형식 판별**: 파일 시그니처(매직 넘버)를 읽어 실제 파일 형식과 그에 대응하는 실제 확장자를 판별한다. (클라이언트가 보낸 MIME 타입은 신뢰하지 않는다.)
2. **이중 확장자 검사**: 파일명에 확장자로 해석될 수 있는 `.` 구분 토큰이 2개 이상 존재하면 거부한다. (예: `invoice.pdf.exe`)
3. **확장자 위장 검사**: 파일명의 확장자와 1번에서 판별한 실제 확장자가 다르면 거부한다. *(파일명에 확장자가 없는 경우 이 단계는 건너뛴다.)*
4. **차단 정책 검사**: 실제 확장자가 현재 차단 정책(체크된 고정 확장자 ∪ 커스텀 확장자)에 포함되면 거부한다.
5. **특수 파일명 치환**: 확장자 없음 / `.`으로 시작 / 매우 긴 파일명 중 하나에 해당하면, 저장용 파일명을 `{현재시간_초단위}.{실제확장자}`로 치환한다. *(이 단계는 1~4번을 모두 통과한 뒤에만 수행한다 — 즉, 이름을 바꾼다고 차단 정책을 우회할 수 없다.)*
6. 저장 후 성공 응답을 반환한다.

> **보안 원칙**: 5번의 파일명 치환은 "표시 편의"를 위한 것이지 정책 우회 수단이 되어서는 안 된다. 반드시 4번(차단 정책 검사)을 실제 확장자 기준으로 통과한 파일에 대해서만 치환·저장한다.

### 4.3 실제 파일 업로드 처리 (백엔드)

| ID | 요구사항 |
|---|---|
| FR-C1 | 업로드 처리 시점마다 최신 차단 정책(고정+커스텀)을 조회해 검증한다 (정책 캐시로 인한 지연 반영 금지). |
| FR-C2 | 차단 대상 업로드 시 4xx 응답과 함께 사유 메시지를 반환한다. |
| FR-C3 | 정상 파일은 스토리지에 저장하고 성공 응답을 반환한다 (저장 위치/방식은 자유 — 로컬 디스크, 오브젝트 스토리지 등). |
| FR-C4 | 저장 경로에 원본 파일명을 그대로 사용하지 않는다(경로 조작/디렉터리 traversal 방지). 저장 파일명과 원본 파일명은 분리 관리한다. |

### 4.4 에러 메시지 정의

| 상황 | 메시지 |
|---|---|
| 커스텀 확장자 중복 추가 | 이미 등록된 확장자입니다 |
| 고정 확장자와 중복 | 고정 확장자에 있는 확장자입니다 |
| 커스텀 확장자 200개 초과 | 최대 200개까지 등록할 수 있습니다 |
| 이중 확장자 | 허용되지 않는 파일명입니다 (이중 확장자) |
| 확장자 위장 | 파일의 실제 형식과 확장자가 일치하지 않습니다 |
| 차단 정책 위반 | 차단된 확장자입니다: .{실제확장자} |

---

## 5. 비기능 요구사항

- **보안**
  - 확장자만으로 파일 형식을 신뢰하지 않고, 매직 넘버 기반 콘텐츠 검증을 반드시 수행한다.
  - 업로드된 파일은 실행 권한 없이 저장하며, 가능하면 웹에서 직접 실행될 수 없는 경로/스토리지에 저장한다.
  - Null byte injection, path traversal(`../`) 등 파일명 조작 공격을 방지한다.
- **동시성**: 동일 확장자를 동시에 추가하는 두 요청이 있어도 중복 저장되지 않도록 DB 레벨 unique 제약을 둔다 (애플리케이션 레벨 검증만으로는 race condition 발생 가능).
- **성능**: 커스텀 확장자 최대 200개 규모에서 목록 조회/중복 검사는 즉시 응답 가능해야 한다.

---

## 6. 데이터 모델 (제안)

### 6.1 `blocked_extension`
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| extension | varchar(20), unique(대소문자 무시) | 확장자 값 (소문자로 정규화 저장) |
| type | enum('FIXED', 'CUSTOM') | 고정/커스텀 구분 |
| is_blocked | boolean | 차단 여부. FIXED는 체크 상태에 따라 갱신, CUSTOM은 등록되는 순간 항상 true |
| created_at / updated_at | datetime | |

- FIXED 7종은 최초 배포 시 seed 데이터로 `is_blocked=false` 상태로 미리 insert 해둔다.
- CUSTOM은 사용자가 "추가"할 때 insert, "X" 클릭 시 delete.

### 6.2 `upload_file` *(선택 — 업로드 이력 추적용)*
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | PK | |
| original_filename | varchar | 사용자가 첨부한 원본 파일명 |
| stored_filename | varchar | 실제 저장된 파일명 (치환된 경우 타임스탬프 기반) |
| detected_extension | varchar | 매직 넘버로 판별한 실제 확장자 |
| status | enum('SUCCESS', 'REJECTED') | |
| reject_reason | varchar, nullable | |
| created_at | datetime | |

---

## 7. API 설계 (제안)

| Method | Endpoint | 설명 |
|---|---|---|
| GET | `/api/extensions/fixed` | 고정 확장자 목록 + 체크 상태 조회 |
| PATCH | `/api/extensions/fixed/{extension}` | 고정 확장자 체크/해제 (`{ "is_blocked": true }`) |
| GET | `/api/extensions/custom` | 커스텀 확장자 목록 조회 |
| POST | `/api/extensions/custom` | 커스텀 확장자 추가 (`{ "extension": "sh" }`) |
| DELETE | `/api/extensions/custom/{id}` | 커스텀 확장자 삭제 |
| POST | `/api/files/upload` | 파일 업로드 (multipart/form-data) |

---

## 8. 오픈 이슈 (결정 필요)

1. **"매우 긴 파일명" 기준**: ~~구체적 글자 수 기준이 제시되지 않았다.~~ **결정됨**: 제안값을 그대로 채택 — 확장자를 제외한 파일명이 100자를 초과하거나, 전체 파일명이 UTF-8 기준 255byte를 초과하는 경우. (`FilenameAnalyzer.isTooLong()`)
2. **커스텀 확장자 입력 형식**: ~~확정 필요~~ **결정됨**: 영문 대소문자 + 숫자만 허용(`^[A-Za-z0-9]+$`), 점(`.`)·공백·특수문자는 모두 불허. 앞뒤 공백은 trim 후 판단하며, trim 결과가 빈 값이거나 20자를 초과하거나 정규식에 맞지 않으면 추가를 거부한다(메시지: "영문/숫자만 입력할 수 있습니다" — 4.4표에 없는 케이스라 별도 문구 사용). 통과한 값은 소문자로 정규화해 저장한다. (`ExtensionService.normalize()`)
3. **삭제(X) 시 확인 팝업 필요 여부**: **결정됨**: 확인 팝업 없이 즉시 삭제(FR-A2-4 문구 그대로 구현). 브라우저 차원의 실행취소는 없다.
4. **업로드 파일 용량 제한** 및 1회 업로드 가능 파일 개수 제한 여부: **결정됨**: 비즈니스 요구사항이 없어 Spring Boot 기본값(1MB/10MB)을 그대로 두는 대신 `spring.servlet.multipart.max-file-size=20MB`, `max-request-size=20MB`로 명시적으로 설정했다. 1회 업로드는 파일 1개로 고정(`multipart` 필드가 단일 `file` 파트만 받음, UI도 파일 1개만 첨부 가능) — 다중 파일 업로드는 범위 밖.
5. **정책 관리 화면 접근 제어**: 관리자 인증/인가는 이번 구현 범위에서 **미결(Out of Scope, 9장 참고)**로 유지했다. `/admin/extensions`는 인증 없이 누구나 접근 가능한 상태이며, 실제 배포 전에는 별도 인증 체계 도입이 필요하다.
6. **이중 확장자 판단 기준**: ~~확정 필요~~ **결정됨**: 파일명 앞의 dot 하나(숨김파일 마커, 있는 경우)만 제외하고, 나머지 이름을 `.` 기준으로 분리했을 때 나오는 확장자 후보 토큰(첫 세그먼트를 제외한 나머지)이 2개 이상이면 이중 확장자로 판단한다 — 즉 파일명 내 모든 `.` 이후 토큰을 후보로 본다(마지막 2개만 보는 방식이 아니다). 예: `my.report.2024.pdf.exe` → 후보 4개 → 이중 확장자로 거부. (`FilenameAnalyzer.isDoubleExtension()`)
7. `.env`처럼 점으로 시작하면서 그 뒤에 또 `.`이 있는 경우(예: `.env.local`): ~~본 문서는 가정했다.~~ **결정됨**: 문서의 가정대로 "점으로 시작하면 확장자 유무와 무관하게 치환 대상"을 그대로 구현했다. `.env.local`은 6번 규칙상 이중 확장자는 아니지만(후보 토큰 1개), 이름이 `.`로 시작하므로 4.2.1 5단계(특수 파일명 치환) 대상이 되어 정책 위반이 없으면 `{epoch}.{실제확장자}`로 저장된다.
8. *(신규)* **PE 계열(`exe/com/scr/cpl`) 및 텍스트 계열(`js/bat/cmd` 등) 확장자의 매직 넘버 한계**: `exe/com/scr/cpl`은 모두 동일한 Windows PE(`MZ`) 헤더를 쓰고, `js/bat/cmd` 등은 애초에 매직 넘버 자체가 없어 콘텐츠만으로는 서로 구분이 불가능하다. **결정됨**: 콘텐츠가 해당 계열(PE 또는 일반 텍스트)에 속함을 확인한 뒤, 파일명이 주장하는 확장자가 그 계열의 합리적인 구성원이면 그대로 실제 확장자로 인정하고, 계열 밖의 확장자를 주장하면 계열의 대표값(PE→`exe`, 텍스트→`txt`)으로 판별해 확장자 위장으로 걸리게 한다. (`FileSignatureDetector`)

---

## 9. Out of Scope

- 관리자 로그인/인증 체계 자체의 설계
- 바이러스/악성코드 백신 스캔 연동
- 업로드된 파일의 미리보기, 다운로드, 파일 목록 조회 UI
- 정책 변경 이력(감사 로그) 관리 화면
